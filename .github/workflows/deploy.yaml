name: CI/CD pipeline RealEstate

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    name: 🚀 Deploy app
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 📝 Create .env.prod for backend
        run: |
          mkdir -p backend
          echo "SECRET_KEY=${{ secrets.SECRET_KEY }}" > backend/.env.prod
          echo "DEBUG=False" >> backend/.env.prod
          echo "POSTGRES_DB=${{ secrets.POSTGRES_DB }}" >> backend/.env.prod
          echo "POSTGRES_USER=${{ secrets.POSTGRES_USER }}" >> backend/.env.prod
          echo "POSTGRES_PASSWORD=${{ secrets.POSTGRES_PASSWORD }}" >> backend/.env.prod
          echo "POSTGRES_HOST=${{ secrets.POSTGRES_HOST }}" >> backend/.env.prod
          echo "POSTGRES_PORT=${{ secrets.POSTGRES_PORT }}" >> backend/.env.prod

      - name: 📝 Create .env.prod for frontend (Vite)
        run: |
          mkdir -p frontend
          echo "VITE_API_BASE_URL=/realestate/api/" > frontend/.env.prod

      - name: 🧱 Build Vite frontend
        run: |
          cd frontend
          npm cache clean --force || true # Utiliser || true pour éviter que l'étape échoue si le cache est vide
          rm -rf node_modules || true # S'assurer que ça ne bloque pas si le dossier n'existe pas
          npm install
          npm run build # Vite construit dans 'dist' par défaut

      - name: 🔒 Correct permissions for dist on CI runner
        run: |
          sudo chmod -R 755 frontend/dist

      - name: 🧹 Clean old project files/folders on VPS
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          script: |
            sudo rm -rf ${{ secrets.VPS_APP_PATH }}/backend/.env.prod
            sudo rm -rf ${{ secrets.VPS_APP_PATH }}/frontend/dist

      - name: 📤 Upload backend .env.prod to VPS
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          source: "backend/.env.prod"
          target: "${{ secrets.VPS_APP_PATH }}/backend/"

      - name: 📤 Upload frontend dist to VPS
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          source: "frontend/dist"
          target: "${{ secrets.VPS_APP_PATH }}/frontend/"

      # - name: 📤 Upload Docker Compose and Nginx config to VPS
      #   uses: appleboy/scp-action@v0.1.4
      #   with:
      #     host: ${{ secrets.VPS_HOST }}
      #     username: ${{ secrets.VPS_USER }}
      #     key: ${{ secrets.VPS_KEY }}
      #     source: |
      #       docker-compose.prod.yaml
      #       nginx_realestate/nginx.conf
      #       backend/Dockerfile.prod
      #       frontend/Dockerfile.prod
      #       backend/entrypoint.sh # Si ce fichier est nécessaire pour le backend
      #       init.sql # Si ce fichier est nécessaire pour la DB
      #     target: "${{ secrets.VPS_APP_PATH }}/"
      - name: 📤 Upload Docker Compose and Nginx config to VPS
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          source: |
            docker-compose.prod.yaml # Fichier à la racine
            init.sql
          target: "${{ secrets.VPS_APP_PATH }}/"

      - name: 📤 Upload Backend Dockerfile and Entrypoint to VPS
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          source: |
            backend/Dockerfile.prod
            backend/entrypoint.sh
          target: "${{ secrets.VPS_APP_PATH }}/backend/"

      - name: 📤 Upload Frontend Dockerfile to VPS
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          source: "frontend/Dockerfile.prod"
          target: "${{ secrets.VPS_APP_PATH }}/frontend/"

      - name: 📤 Upload Nginx config folder to VPS
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          source: "nginx_realestate/nginx.conf"
          target: "${{ secrets.VPS_APP_PATH }}/nginx_realestate/"
          
      - name: 🔐 SSH to VPS & deploy
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          script: |
            cd ${{ secrets.VPS_APP_PATH }}
            mkdir -p backend frontend nginx_realestate
            docker compose -f docker-compose.prod.yaml down --remove-orphans || true
            docker compose -f docker-compose.prod.yaml up -d --build
            docker image prune -f
            docker compose -f docker-compose.prod.yaml exec -T backend python manage.py migrate --noinput
            docker compose -f docker-compose.prod.yaml exec -T backend python manage.py collectstatic --noinput
            echo "Success deploy !" # Message de fin de déploiement Docker

      - name: 🔁 Reload Nginx on VPS
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_KEY }}
          script: |
            sudo systemctl reload nginx